# NAT (network address translation)
class abc로하기어렵고(환경이바뀌어서) 동적ip같은거나옴
라우터기준으로 안에서 구분을 잘해주면 ip를 굳이 할당해야할까? 외부는 ip할당하고 내부는 따로 테이블을 구분할수있으면 ip할당을 안해도?
안쪽을 로컬네트워크라고 함. 외부에서는 그 ip를 알 필요가 없음.

호스트에서 라우터안쪽 망에서 장치가 보내면 NAT 테이블 관리 . 도착지는 외부망 ip. 그러면 라우터가 내부의 어쩌고가 외부에 보낼건데 -> 라우터가 테이블로 매핑해서 외부에서 알수있는 라우터자신의 ip를 적어줌. 소스주소가달라짐. 목적지는똑같고.

단점: 복잡 지연시간들어감. 외부에사 몰라서 보안면에는 좋은데 p2p때 방화벽때문에 불편 -> ipv6로 감

# ipv6
ip datagram 포맷에서 불팔요한 필드 없앰.
헤더길어짐

- flow label:openflow. flow단위 관리.
- nextheader
- hop limit어느정도hop까지가능한지
- ip주소 4바이트 -> 16바이트(128bit)(4줄)
  - 줄 위부터 4,4,16,16 -> 40(원래 20ㅇ었음)

없어진거
- fragmenataion 4byte 없어짐
장비표준화되면서 frag해야하는환경이별로없어짐.
- checksum 없어짐
실제link의 bit error이제 잘안남(구리->광케이블안정젇) 오버헤드

에러나면? 재전송하면됨

## Tunneling
전세계라우터 한번에 ipv6로 바꿀수없음
헤더가 달라도 ipv6위에 ipv4헤더 붙여주고 다시 ipv6갈때 헤더뗌

# SDN


# 라우팅 프로토콜
최적화된길 알려줌.
라우터와 링크의 연결을 그래프로 표현가능. graph G = (N, E) N={...} E={(,),(,),(,)}

- 글로벌: linnk state algorithm
- decentralized: distance vector alg.

- static:관리자가수동으로
- dynamic:동적으로

# link state 알고리즘
- Dijkstra's
  - 다이렉트링크가 있/없 cost 구분.없으면 무한대 표시.
  - 현재값
  - 이전노드
  - 노드의 set

문제: oscillations

# Distance vector 알고리즘
bellman-ford
인접한노드와 알고있는거리더한것들중최솟값

나중에다시보자